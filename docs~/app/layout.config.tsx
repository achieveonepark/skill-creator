import { createBaseOptions } from 'ach-fumadocs-theme';

// 공통 테마에서 nav 로고/타이틀, GitHub 링크 등 통일 요소를 가져온다.
export const baseOptions = createBaseOptions({
  title: 'AchReactive',
  githubUrl: 'https://github.com/achieveonepark/AchReactive',
});
